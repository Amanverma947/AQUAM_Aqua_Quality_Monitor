<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="buttons.storytelleraman.com.aquam">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feedback_window"></activity>
        <activity android:name=".Final_Details" />
        <activity android:name=".Check_Quality" />
        <activity android:name=".Add_Device" />
        <activity android:name=".Add_City" />
        <activity android:name=".AdminOptions" />
        <activity android:name=".Admin_panel_login" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Splash_Screen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>